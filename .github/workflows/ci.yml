name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          path: hazyhaar_pkg
      - uses: actions/checkout@v6
        with:
          repository: hazyhaar/pdfast
          path: pdfast
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: hazyhaar_pkg/go.sum
      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
      - name: Lint
        working-directory: hazyhaar_pkg
        run: golangci-lint run --timeout=5m

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          path: hazyhaar_pkg
      - uses: actions/checkout@v6
        with:
          repository: hazyhaar/pdfast
          path: pdfast
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: hazyhaar_pkg/go.sum
      - name: Download dependencies
        working-directory: hazyhaar_pkg
        run: go mod download
      - name: Build
        working-directory: hazyhaar_pkg
        run: CGO_ENABLED=0 go build ./...
      - name: Test
        working-directory: hazyhaar_pkg
        run: go test -race -v -count=1 -timeout 120s ./...

  ci-ok:
    name: CI OK
    needs: [lint, test]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - run: |
          if [[ "${{ needs.lint.result }}" != "success" ]] || [[ "${{ needs.test.result }}" != "success" ]]; then
            echo "CI failed: lint=${{ needs.lint.result }} test=${{ needs.test.result }}"
            exit 1
          fi
